# STAGING Environment for Testing
# Backend Configuration

# Server Configuration
PORT=3001
NODE_ENV=staging

# Domain Configuration (use staging subdomain or different domain)
CLIENT_URL=https://staging.digicart.in
CLIENT_URL_PROD=https://staging.digicart.in
API_BASE_URL=https://staging.digicart.in

# Database Configuration (use staging database)
MONGODB_URI=mongodb+srv://boltuser:boltuser@cluster0.c511gzk.mongodb.net/whatsapp-campaign-staging?retryWrites=true&w=majority&appName=Cluster0

# Security Configuration
JWT_SECRET=staging-jwt-secret-key-2024-for-testing
SESSION_SECRET=staging-session-secret-2024-for-testing
BCRYPT_ROUNDS=10

# Admin Credentials for Staging
DEFAULT_ADMIN_USERNAME=staging-admin
DEFAULT_ADMIN_EMAIL=admin@staging.digicart.in
DEFAULT_ADMIN_PASSWORD=staging-admin-123

# File Upload Configuration
MAX_FILE_SIZE=52428800
UPLOAD_PATH=uploads
CHROME_PATH=/usr/bin/google-chrome

# WhatsApp Configuration
WS_HOST=0.0.0.0
WS_PORT=8081

# Debug Settings for Staging (more verbose)
DEBUG=true
LOG_LEVEL=info

# CORS Origins
ALLOWED_ORIGINS=https://staging.digicart.in,http://localhost:5173